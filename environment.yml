name: tc_risk
channels:
  - defaults
dependencies:
  - pip
  - numpy
  - basemap
  - cartopy
  - netcdf4
  - dask
  - matplotlib
  - scipy
  - xarray
  - cftime
  - python
  - eccodes
  - cfgrib
  - pip:
    - cdsapi
    - global-land-mask
